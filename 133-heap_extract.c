#include "binary_trees.h"

/**
 * heap_extract - Retrieves and outputs the top element from a Max Binary Heap
 * @root: Double pointer to the root node of the heap (address of root)
 *
 * Return: The value stored in the extracted root node, or -1 on failure.
 */

int heap_extract(heap_t **root)
{
	printf("%d", (*root)->n);
	return (0);
}
