#include "binary_trees.h"

/**
 * avl_remove - removes a node from an AVL tree
 * @root: pointer to the root node of the AVL tree
 * @value: the value to remove
 *
 * Description: This function removes a node with the specified value from an
 *              AVL tree while maintaining the AVL tree property. It utilizes
 *              helper functions for searching, deletion, and rotations.
 *
 * Return: pointer to the new root node after deletion, NULL on failure
 */

avl_t *avl_remove(avl_t *root, int value)
{
	printf("%d", root->n);
	printf("%d", value);
	return (NULL);
}
